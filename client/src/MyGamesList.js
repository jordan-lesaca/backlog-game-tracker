

function MyGamesList({ user, games, editGame, removeGame, addGame}){

    return(
        <div>

        </div>
    )
}

export default MyGamesList


/* <GameForm key={games.id} user={user} addGame={addGame}/>  */

/* <MyGamesCard
games={games} 
key={games.id} 
user={user}
editGame={editGame}
removeGame={removeGame}
/> */